<?php
declare(strict_types=1);

// Nofrixion SDK utility: prepare_path

class NofrixionPreparePath
{
    public static function call(NofrixionContext $ctx): string
    {
        $point = $ctx->point;
        $parts = [];
        if ($point) {
            $p = \Voxgig\Struct\Struct::getprop($point, 'parts');
            if (is_array($p)) {
                $parts = $p;
            }
        }
        return \Voxgig\Struct\Struct::join($parts, '/', true);
    }
}
